syntax = "proto3";

option java_multiple_files = true;

package grpcService;
import "model.proto";

service UrgentBloodTransferGrpcService {
  rpc transfer (UrgentBloodTransferRequest) returns (UrgentBloodTransferResponse);
}
